# 开发日志

## Python 修复框架

- OBJ 读取器保留 `o`、`g`、`usemtl`，不再把装配体身份丢掉。
- 原始网格先按共享边拆分，焊点只在零件内部进行。
- 增加 `assembly`、`surface`、`solid` 三种模式。
- `surface` 模式处理退化面、重复面、绕序、非流形面扇和小孔。
- `solid` 模式使用 Manifold3D 的 64 位接口合并闭合零件。
- point-cloud-utils 只作为显式近似重建，不自动改变几何。
- 删除 CGAL、Gmsh 和 PyMeshFix 主流程。
- 四个真实装配体模型已加入自动回归测试。

## 第一阶段四面体生成与验收

- 增加非相邻三角形自相交诊断，区分真实穿透和浮点近邻接缝。
- 按 Gmsh 离散表面分类、几何重建、体网格生成流程输出一阶四面体。
- 增加 Netgen 优化、翻转/退化/重复单元和体边界一致性检查。
- 增加 `mean-ratio`、双向表面偏差、表面体积与四面体总体积误差。
- 输出带物理组的 `.msh` 和带逐单元质量的 `.vtk`。
- 第一版只接受单材料区域，多材料输入会明确拒绝。

## 严格边界锁定

- 合法闭合实体默认跳过修复和布尔重建，原样保留输入 `V/F` 与 OBJ 面标签。
- 增加 TetGen 严格模式，禁止边界分裂，只允许增加内部 Steiner 点。
- 严格模式禁止焊点、补洞、表面均匀细分和近似重建。
- 增加边界逐点逐面验收和 SHA-256 证据，边界变化直接作为硬错误。
- `.msh` 继承 OBJ 的 `g` 或 `o` 边界物理组，便于后续施加边界条件。
- Gmsh 体网格保留为显式 `relaxed` 模式，不再与边界不变混为一谈。

## 保持几何的表面质量重剖分

- 增加最小角、三角形 `mean-ratio` 和等边参考形状条件数指标。
- 按 OBJ 标签识别共面片，保留原直线特征边，删除片内劣质对角线。
- 使用 Gmsh PlaneSurface Frontal-Delaunay 在平面内部重新布点和剖分。
- 重剖分后检查特征边偏差、离面偏差、片区面积和闭合体积误差。
- 对原几何尖角和极短特征边造成的不可消除坏单元明确失败，不自动改外形。
- 增加面数预估和硬上限，避免极小目标尺寸导致不受控的网格规模。

## 基坑表面重剖分试验与已知问题

- 原始 `pit_overlap.obj` 是封闭流形：边界边、非流形边和自交均为 0。
- 直接锁定原几何进行 Gmsh 重剖分后仍保持自交为 0，但有 417 个表面坏面，
  占 1.173%；极短特征边仍会强制产生近零最小角。
- 试验性 Manifold 容差简化把坏面降到 127 个，但在
  `x≈1.5、y≈14、z∈[-29,-28]` 的连接角制造了局部面折叠。
- 现有自交检测跳过共享顶点的相邻面，未发现 7 组相邻共面重叠；
  Gmsh 细分后表现为 52 组共面面积重叠和 24 组非共面相交。
- 该试验输出只用于观察表面质量，未通过几何验收，禁止用于 CAE
  四面体生成和 3D 打印。
- 后续必须增加相邻面折叠检查，并使用带流形、翻转、自交、体积和边界
  偏差约束的短边处理；不能使用“简化后与自身布尔并集”作为正式修复。

## 基坑 CAE 体网格问题定位与修复

- 新增共享顶点、共享边相邻三角面的面积覆盖检测；原先会漏报的局部折叠
  现在会以 `adjacent_face_overlaps` 拒绝。
- 定位到低质量四面体并非普通长三角形导致，而是布尔并集后的表面在
  `x≈1.5、z=-29` 保留了约 `9.33e-7` 宽的数值伪薄带。
- Delaunay、Frontal/Netgen、HXT 三种 Gmsh 体算法均在同一边界约束处
  产生 4 个薄楔；体内节点平滑不能修复边界上的近零特征。
- 禁止在最终闭合壳上直接全局吸附坐标；该操作会制造退化面、T 形接缝
  和相邻面覆盖。
- 新增显式 `--pre_union_snap_rel`：只在多零件布尔并集前统一近重合
  坐标面，保留装配体包围盒，每个零件先保持闭合，再重新执行实体并集。
- 基坑使用 `3e-8` 相对容差后，四面体数为 20552，最小 mean-ratio
  从 `4.541e-5` 提升到 `0.33952`，低于 `0.05` 的单元由 4 个降为 0；
  翻转、零体积、重复单元和边界错配均为 0。
- 新实体相对旧修复壳的离散双向最大偏差约 `2.77e-8`，体积相对差约
  `1.35e-7`；该容差会删除小于阈值的几何特征，因此默认仍为关闭，
  必须由用户按建模公差显式启用。
