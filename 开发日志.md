* 收集实验室遗留的“问题 OBJ 模型”，用于全面测试修复算法的极限和系统的稳定性
* 补充 Web 后端模块（基于 FastAPI），实现“接收文件 → 调用现有修复代码 → 返回结果”的核心闭环
* 补充极简前端页面，仅保留上传按钮、下载触发和状态提示，并在显眼处注明“当前仅支持纯几何修复”
* 完善异常处理逻辑，确保遇到极端破损模型导致底层 C++ 失败时，网页服务能返回报错提示而不是直接死机崩溃
* 更新项目 README 文档，添加网页端的启动说明、新增的依赖环境以及操作截图
* 明确项目后续补全方向：通过曲率实现定向定向加密；材质，纹理信息的保留探索