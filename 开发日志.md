# 开发日志

## Python 修复框架

- OBJ 读取器保留 `o`、`g`、`usemtl`，不再把装配体身份丢掉。
- 原始网格先按共享边拆分，焊点只在零件内部进行。
- 增加 `assembly`、`surface`、`solid` 三种模式。
- `surface` 模式处理退化面、重复面、绕序、非流形面扇和小孔。
- `solid` 模式使用 Manifold3D 的 64 位接口合并闭合零件。
- point-cloud-utils 只作为显式近似重建，不自动改变几何。
- 删除 CGAL、Gmsh 和 PyMeshFix 主流程。
- 四个真实装配体模型已加入自动回归测试。

## 第一阶段四面体生成与验收

- 增加非相邻三角形自相交诊断，区分真实穿透和浮点近邻接缝。
- 按 Gmsh 离散表面分类、几何重建、体网格生成流程输出一阶四面体。
- 增加 Netgen 优化、翻转/退化/重复单元和体边界一致性检查。
- 增加 `mean-ratio`、双向表面偏差、表面体积与四面体总体积误差。
- 输出带物理组的 `.msh` 和带逐单元质量的 `.vtk`。
- 第一版只接受单材料区域，多材料输入会明确拒绝。
